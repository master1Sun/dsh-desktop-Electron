node_modules
dist
dist-release
out
.DS_Store
.eslintcache
*.log*

# bundled Node runtime binary (fetched by npm run setup:node)
resources/node/

# bundled openclaw CLI (fetched by npm run setup:openclaw / prebuild, ~537MB)
resources/openclaw/
resources/dsh/

# bundled codex CLI (fetched by npm run setup:codex / prebuild, ~389MB)
resources/codex/

# installed page projects (user-managed, keep the folder itself)
pages/*
!pages/.gitkeep

# vitest
.vitest.tmp/
test/tmp/

# macOS icon source folder (regenerate icon.icns via build/make-icns.sh on a Mac)
build/icon.iconset/
/.workbuddy
