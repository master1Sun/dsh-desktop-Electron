{
  "name": "OpenClaw Gateway",
  "description": {
    "zh": "由容器管理的 openclaw gateway（自带最新版），主界面内嵌打开 Control UI；配置目录默认 ~/.openclaw",
    "en": "Container-managed openclaw gateway (bundled latest); the main window embeds its Control UI; the config dir defaults to ~/.openclaw"
  },
  "kind": "openclaw",
  "openclaw": {
    "port": 18789
  },
  "envVars": [
    {
      "key": "OPENCLAW_HOME",
      "label": {
        "zh": "OPENCLAW 配置目录",
        "en": "OPENCLAW config dir"
      },
      "defaultPath": "~/.openclaw",
      "description": {
        "zh": "留空即使用 openclaw CLI 的默认目录 ~/.openclaw，与终端共用同一套配置",
        "en": "Empty uses openclaw’s default ~/.openclaw — the same config store the CLI uses"
      }
    }
  ]
}