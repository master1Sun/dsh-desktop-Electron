{
  "name": "DSH (web)",
  "description": {
    "zh": "由容器管理的 @deepseek-ai/dsh profile「web」，插件在本机 userData 的 profile 目录内管理",
    "en": "Container-managed @deepseek-ai/dsh profile \"web\"; plugins are managed in this machine’s userData profile directory"
  },
  "kind": "dsh",
  "dsh": {
    "profile": "web",
    "port": 8899
  },
  "envVars": [
    {
      "key": "DSH_HOME",
      "label": {
        "zh": "DSH 配置目录",
        "en": "DSH config directory"
      },
      "defaultPath": "~/.dsh",
      "description": {
        "zh": "留空即使用 dsh CLI 的默认目录 ~/.dsh，与终端里的 dsh 共用同一套 profile",
        "en": "Leave empty to use dsh's default ~/.dsh — the same profile store the CLI uses"
      }
    }
  ]
}